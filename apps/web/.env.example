# Base URL of the apps/api Worker for this environment, no trailing slash.
# Set as a Cloudflare Pages environment variable per branch (see docs/setup-checklist.md);
# .env.development supplies this for `npm run dev`.
VITE_API_URL=https://api.drsrisushmadentalclinic.com
